<style>
  .avm-cta {
    background: #0A1628;
    padding: 100px 0;
    position: relative;
    overflow: hidden;
  }

  .avm-cta__inner {
    max-width: 1280px;
    margin: 0 auto;
    padding: 0 3rem;
    position: relative;
    z-index: 2;
    max-width: 600px;
    margin-left: 3rem;
  }

  .avm-cta__label {
    display: inline-block;
    font-family: 'DM Sans', sans-serif;
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: #2563EB;
    margin-bottom: 1.4rem;
  }

  .avm-cta__h2 {
    font-family: 'DM Serif Display', Georgia, serif;
    font-size: clamp(34px, 4vw, 52px);
    font-weight: 400;
    color: #ffffff;
    letter-spacing: -0.03em;
    line-height: 1.08;
    margin: 0 0 1.4rem;
  }

  .avm-cta__sub {
    font-family: 'DM Sans', sans-serif;
    font-size: 16px;
    color: rgba(255, 255, 255, 0.55);
    line-height: 1.75;
    max-width: 480px;
    margin: 0;
  }

  .avm-cta__btn {
    display: inline-flex;
    align-items: center;
    margin-top: 2.5rem;
    font-family: 'DM Sans', sans-serif;
    font-size: 15px;
    font-weight: 500;
    background: #2563EB;
    color: #ffffff;
    padding: 14px 32px;
    border-radius: 6px;
    text-decoration: none;
    border: none;
    cursor: pointer;
    transition: background 0.18s ease;
    min-height: 44px;
  }

  .avm-cta__btn:hover { background: #1d4ed8; color: #ffffff; }

  /* Decorative number */
  .avm-cta__deco {
    position: absolute;
    right: 4rem;
    top: 50%;
    transform: translateY(-50%);
    font-family: 'DM Serif Display', Georgia, serif;
    font-size: 200px;
    font-weight: 400;
    color: rgba(255, 255, 255, 0.04);
    line-height: 1;
    pointer-events: none;
    user-select: none;
    letter-spacing: -0.04em;
    z-index: 1;
    white-space: nowrap;
  }

  @media screen and (max-width: 1024px) {
    .avm-cta__deco { font-size: 140px; right: 2rem; }
    .avm-cta__inner { margin-left: 2rem; }
  }

  @media screen and (max-width: 767px) {
    .avm-cta { padding: 64px 0; }
    .avm-cta__inner { margin-left: 0; max-width: none; padding: 0 1.5rem; }
    .avm-cta__deco { display: none; }
  }
</style>

<section class="avm-cta">
  <div class="avm-cta__inner">
    <span class="avm-cta__label">Get started</span>
    <h2 class="avm-cta__h2">Request our detailed product catalogue</h2>
    <p class="avm-cta__sub">Over 3,400 surgical instruments across 6 specialties. Our team will send the full catalogue to your inbox within one business day.</p>
    <a href="/pages/request-catalogue" class="avm-cta__btn">Request full catalogue &rarr;</a>
  </div>
  <span class="avm-cta__deco" aria-hidden="true">3,400</span>
</section>

{% schema %}
{
  "name": "AVM Catalogue CTA",
  "settings": [],
  "presets": [{ "name": "AVM Catalogue CTA" }]
}
{% endschema %}
